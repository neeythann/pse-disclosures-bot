[project]
name = "pse-disclosures"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.14.2",
    "requests>=2.32.5",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]


[tool.pytest.ini_options]
testpaths = [
    "./tests",
]
